<% provide(:title, "#{@g}年生") %>
<!DOCTYPE html>
<html>
  <body>
    <div class="container">
      <div class="grades">
        <%= render 'layouts/grades_list' %>
      </div>
      <div class="articles">
        <%= render 'grades/grade_list' %>
      </div>
    </div>
  </body>
