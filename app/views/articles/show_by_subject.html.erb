<% provide(:title, "#{@s.name}") %>
<!DOCTYPE html>
<html>
  <body>
    <div class="container">
      <div class="grades">
        <%= render 'layouts/grades_list' %>
      </div>
      <div class="subjects">
        <%= render 'layouts/subjects_list' %>
      </div>
      <div class="articles">
        <%= render 'grades/grade_list' %>
      </div>
    </div>
  </body>
